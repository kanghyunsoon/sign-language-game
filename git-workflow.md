# Git Workflow 지침 (에이전트용)

이 문서는 이 저장소에서 기능 개발을 진행할 때 반드시 따라야 하는 Git 워크플로우와 작업 절차를 정의합니다. 에이전트는 아래 순서와 규칙을 그대로 따라야 합니다.

이 저장소는 **GitLab**(`lab.ssafy.com`)에서 관리되며, MR(Merge Request) 생성 등 원격 작업은 GitLab CLI인 **`glab`**을 통해 수행한다.

### glab 사전 준비

- MR 생성 전에 `glab auth status`로 `lab.ssafy.com` 호스트에 인증되어 있는지 확인한다.
- 인증되어 있지 않으면 에이전트가 대신 로그인할 수 없으므로, 사용자에게 `! glab auth login --hostname lab.ssafy.com`을 직접 실행해달라고 안내한다.

## 0. 개발 시작 전: 계획 제시 및 승인

- 어떤 기능 개발/버그 수정 작업이든 **코드를 작성하기 전에** 다음을 사용자에게 제시한다.
  - 작업 목표와 범위
  - 변경할 파일/모듈 개요
  - 생성할 브랜치 이름
  - 예상 커밋 단위(어떻게 쪼갤지)
- 사용자가 **명시적으로 승인**한 뒤에만 실제 개발(코드 작성)을 시작한다.
- 승인 없이 코드를 먼저 작성하지 않는다.

## 1. 브랜치 구조

### 브랜치 계층 (중요)

이 저장소는 영역별 통합 브랜치를 두는 구조를 사용한다. 평소 개발 흐름은 다음과 같다.

```
master
 └─ develop
     ├─ ai        (AI 영역 통합 브랜치)
     ├─ backend   (백엔드 영역 통합 브랜치)
     └─ frontend  (프론트엔드 영역 통합 브랜치)
          └─ feature/bugfix/... (개별 기능·버그 브랜치)
```

- `master ← develop ← (ai / backend / frontend) ← 각 기능 브랜치` 순으로 병합된다.
- **기능/버그 브랜치는 `develop`에서 직접 분기하지 않는다.** 반드시 작업 영역에 해당하는 `ai` / `backend` / `frontend` 브랜치에서 분기하고, 완료 후 다시 그 영역 브랜치로 병합(MR)한다.
- `ai` / `backend` / `frontend` 브랜치를 `develop`로 병합하는 것은 각 영역 작업이 충분히 쌓이고 안정화된 시점에 별도로 진행하며, 이 병합은 사용자 승인을 받아 진행한다(기능 단위 MR과는 별개의 상위 병합).

### Main Branches
- `main`(또는 `master`): 항상 배포 가능한 안정 상태. 직접 커밋 금지.
- `develop`: 다음 릴리즈를 위한 통합 브랜치. `ai` / `backend` / `frontend` 브랜치들이 최종적으로 병합되는 대상. 직접 커밋 금지.

### 영역 통합 브랜치 (Domain Integration Branches)
| 브랜치 | 용도 | 분기 출발점 | 병합 대상 |
| --- | --- | --- | --- |
| `ai` | AI 관련 기능들의 통합 | `develop` | `develop` |
| `backend` | 백엔드 기능들의 통합 | `develop` | `develop` |
| `frontend` | 프론트엔드 기능들의 통합 | `develop` | `develop` |

### Supporting Branches
| 타입 | 용도 | 분기 출발점 | 병합 대상 |
| --- | --- | --- | --- |
| `feature` | 신규 기능 개발 | 해당 영역 브랜치(`ai`/`backend`/`frontend`) | 분기한 영역 브랜치 |
| `bugfix` | 릴리즈 전 버그 수정 | 해당 영역 브랜치(`ai`/`backend`/`frontend`) | 분기한 영역 브랜치 |
| `hotfix` | 릴리즈 후 `main`의 긴급 버그 수정 | `main` | `main` + `develop`(+ 필요 시 관련 영역 브랜치) |
| `release` | 릴리즈 준비(QA, 버전 등 메타데이터 수정) | `develop` | `main` + `develop` |

작업 완료 후 병합된 supporting 브랜치는 삭제하는 것을 원칙으로 한다(삭제는 사용자 승인 후 수행).

### 브랜치 네이밍 규칙

```
<타입>/<이슈번호>-<설명>
```

예: `feature/123-user-login`, `bugfix/456-fix-null-check`, `hotfix/789-payment-crash`

- 이슈 번호가 없는 경우, 설명만으로 네이밍하되 이슈 트래커 연동이 가능하면 이슈 번호를 우선 포함한다.
- 설명은 영문 소문자 + 하이픈(kebab-case)으로 간결하게 작성한다.

### 브랜치 생성 원칙

- **기능 개발을 시작하기 전에는 반드시 먼저 브랜치를 생성한다.** `feature`/`bugfix`는 작업 영역에 해당하는 `ai`/`backend`/`frontend` 브랜치에서(hotfix는 `main`, release는 `develop`에서) 최신 상태를 pull 받은 뒤 분기한다.
- 어떤 영역 브랜치에서 분기해야 할지 애매하면 코드를 작성하기 전에 사용자에게 확인한다.
- 하나의 브랜치에는 하나의 논리적 작업 단위만 담는다. 여러 기능을 섞지 않는다.

## 2. 커밋 컨벤션

### 커밋 유형 (영어 대문자로 명시)

| 유형 | 의미 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 formatting, 세미콜론 누락 등 코드 자체 변경 없는 경우 |
| `refactor` | 코드 리팩토링 |
| `test` | 테스트 코드 추가/리팩토링 |
| `chore` | 패키지 매니저 수정, 기타(.gitignore 등) |
| `design` | CSS 등 사용자 UI 디자인 변경 |
| `comment` | 필요한 주석 추가/변경 |
| `rename` | 파일/폴더명 변경 또는 이동만 수행 |
| `remove` | 파일 삭제만 수행 |
| `!BREAKING CHANGE` | 큰 API 변경 |
| `!HOTFIX` | 치명적 버그 긴급 수정 |

### 커밋 메시지 규칙

- 형식: `<유형>: <제목>`
- 제목과 본문은 빈 줄로 분리한다.
- 제목/본문은 한글로 작성하여 내용을 명확히 전달한다.
- 제목은 명사형으로 간결하게 끝맺고, 끝에 마침표(`.`)를 붙이지 않는다.
- 제목은 영문 기준 50자 이내로 작성한다.
- 본문에는 **무엇을 왜** 변경했는지 설명한다(어떻게는 코드가 보여준다).
- 변경 항목이 여러 개면 글머리 기호(`-`)로 나열한다.
- 코드가 직관적으로 이해될 것이라 가정하지 않고, 필요한 맥락을 본문에 남긴다.

### 커밋 단위 원칙

- **한 커밋에는 한 가지 문제만** 담는다. 추적 가능성을 위해 기능 단위로 커밋을 쪼갠다.
- 서로 다른 관심사(예: 기능 추가 + 스타일 수정 + 문서 수정)를 한 커밋에 섞지 않는다.
- 큰 기능은 의미 있는 작은 단위(예: 모델 추가 → 서비스 로직 → 컨트롤러 → 테스트)로 나눠 순차적으로 커밋한다.

## 3. 개발 완료 전 테스트

기능 구현을 마치고 커밋/푸시하기 전, 반드시 아래 순서를 따른다.

1. **테스트 코드가 존재하는 경우**: 관련 테스트를 직접 실행하여 통과 여부를 확인한다.
   - 백엔드(Gradle) 예시: `./gradlew test` 또는 변경된 모듈 한정 테스트 실행.
   - 테스트가 실패하면 원인을 분석하고 수정한 뒤 재실행한다. 실패한 채로 커밋하지 않는다.
2. **테스트 코드가 존재하지 않는 경우**: 에이전트가 임의로 테스트를 생략하지 않는다. 사용자에게 다음을 안내하고 **수동 검증을 요청**한다.
   - 변경 사항이 무엇인지
   - 어떤 시나리오를 직접 확인해야 하는지(golden path + 엣지 케이스)
   - 사용자가 검증을 완료했다고 확인해줄 때까지 커밋을 진행하지 않는다.
3. 테스트(또는 사용자 검증)가 완료된 후에만 4단계(커밋)로 진행한다.

## 4. 커밋 → 푸시 → MR 생성

테스트/검증이 끝나면 아래를 순서대로 자동 진행한다.

1. **커밋**: 위 커밋 컨벤션에 따라 기능 단위로 나눠 커밋한다. 파일은 관련된 것만 명시적으로 `add` 한다(`git add -A` 지양).
2. **푸시**: 원격에 현재 작업 브랜치를 push한다(신규 브랜치는 `-u` 옵션으로 업스트림 설정).
   ```
   git push -u origin <브랜치명>
   ```
3. **대상 브랜치 확인**: 이 저장소에는 `ai`, `backend`, `frontend` 등 영역별 통합 브랜치가 별도로 존재한다. `glab mr create`를 실행하기 전, 이번 작업이 어느 영역에 해당하는지(즉 MR을 `ai` / `backend` / `frontend` 중 어느 브랜치로 올려야 하는지) **사용자에게 반드시 먼저 물어본다**. 파일 변경 내역으로 영역이 명확해 보여도 임의로 단정하지 않고 확인을 받은 뒤 진행한다.
4. **MR 생성**: 3번에서 확인한 대상 브랜치와 아래 5절 규칙에 따라 `glab mr create`로 MR을 생성한다.

> 커밋/푸시/MR 생성 각각은 원격 저장소 상태를 바꾸는 작업이므로, 사전에 사용자가 이 워크플로우 전체를 승인한 경우가 아니라면 실행 전 간단히 진행 사실을 알린다. 별도 지시가 없다면 0단계에서 받은 승인을 이 전체 흐름(테스트→커밋→푸시→MR)에 대한 승인으로 간주하고 순차 진행한다.

## 5. MR(Merge Request) 작성 규칙

- **대상 브랜치**: 1절의 브랜치 계층에 따라 MR을 연다.
  - `feature`, `bugfix` → 분기한 영역 브랜치(`ai` / `backend` / `frontend`) — 4절 3단계에서 사용자에게 확인한 브랜치를 사용한다.
  - `hotfix`, `release` → `main` (병합 후 `develop`에도 별도 반영)
  - `ai` / `backend` / `frontend` → `develop`으로의 병합은 영역 작업이 안정화된 시점에 별도로 진행하며, 사용자 승인 후에만 수행한다.
- **제목**: 커밋 제목 컨벤션과 동일하게 `<유형>: <내용>` 형식, 50자 이내, 한글로 명확하게.
- **본문 구성**:
  ```
  ## 개요
  - 이 MR에서 무엇을, 왜 변경했는지 요약

  ## 변경 사항
  - 변경 내용 1
  - 변경 내용 2

  ## 테스트
  - [ ] 실행한 테스트 또는 수동 검증 항목 체크리스트

  ## 관련 이슈
  - #이슈번호 (있는 경우)
  ```
- **생성 명령어**: `glab mr create`를 사용하며, 본문은 HEREDOC으로 전달해 줄바꿈/마크다운이 깨지지 않도록 한다.
  ```
  glab mr create \
    --source-branch <브랜치명> \
    --target-branch <develop 또는 main> \
    --title "<유형>: <내용>" \
    --description "$(cat <<'EOF'
  ## 개요
  - ...

  ## 변경 사항
  - ...

  ## 테스트
  - [ ] ...

  ## 관련 이슈
  - #이슈번호
  EOF
  )" \
    --remove-source-branch
  ```
  - 병합 후 소스 브랜치를 자동 삭제하지 않으려면 `--remove-source-branch` 옵션을 생략한다(1절의 "병합 후 브랜치 삭제는 사용자 승인 후"와 상충하지 않도록, 팀 정책상 즉시 자동 삭제를 원치 않으면 이 옵션을 빼고 병합 후 수동으로 삭제 여부를 확인한다).
  - 초안 상태로 먼저 올리고 싶다면 `--draft` 옵션을 추가한다.
  - 리뷰어/담당자 지정이 필요하면 `--reviewer <username>`, `--assignee <username>` 옵션을 추가한다.
- MR 생성 후 `glab mr create`가 출력하는 URL을 사용자에게 공유한다.
- MR 생성은 원격에 공개적으로 노출되는 작업이므로, 생성 직전 어떤 내용으로 MR을 열 것인지 요약해 알린 뒤 진행한다.

## 6. 요약: 표준 작업 흐름

1. 작업 요청 접수 → 계획(브랜치명, 범위, 커밋 단위) 수립 후 사용자에게 제시
2. 사용자 승인
3. `develop`(또는 `main`) 최신화 후 브랜치 생성
4. 기능 단위로 나눠 구현 + 커밋 (컨벤션 준수)
5. 테스트 존재 시 자동 실행 및 통과 확인 / 부재 시 사용자에게 수동 검증 요청 후 대기
6. 검증 완료 확인
7. 커밋 정리 → 푸시 → **대상 브랜치(`ai`/`backend`/`frontend`) 사용자 확인** → `glab mr create`로 MR 생성 (5절 규칙 준수)
8. MR URL 공유 및 결과 보고
