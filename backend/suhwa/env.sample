# Server
SERVER_PORT=8080

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=suhwa
DB_USERNAME=root
DB_PASSWORD=

# JPA
JPA_DDL_AUTO=none
JPA_SHOW_SQL=false
JPA_FORMAT_SQL=false

# JWT (HS256 서명 키 — 최소 32바이트 이상의 임의 문자열 필요)
JWT_SECRET=
JWT_ACCESS_EXPIRATION_MS=3600000
JWT_REFRESH_EXPIRATION_MS=1209600000
