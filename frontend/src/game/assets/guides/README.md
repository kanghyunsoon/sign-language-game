# Symbol Guide Assets

Place optional approved guide assets here using a stable path referenced by `GameSymbolMetadata.guideAsset`.

The registry currently sets every `guideAsset` to `null` and every `templateAvailable` flag to `false`: no reference pose is fabricated. A future approved asset or captured template must update the metadata deliberately.
