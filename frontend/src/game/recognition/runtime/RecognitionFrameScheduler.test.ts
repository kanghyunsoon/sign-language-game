import {describe,expect,it,vi} from "vitest";import {BrowserRecognitionFrameScheduler} from "./RecognitionFrameScheduler";
describe("BrowserRecognitionFrameScheduler",()=>{it("limits render, hand and pose independently and cleans the frame",()=>{let callback:FrameRequestCallback=()=>undefined;let at=0;const cancel=vi.fn();const scheduler=new BrowserRecognitionFrameScheduler({video:{readyState:4,currentTime:0} as HTMLVideoElement,config:{renderFps:60,handTrackingFps:20,poseTrackingFps:10,aiInferenceFps:12},now:()=>at,requestFrame:(next)=>{callback=next;return 7;},cancelFrame:cancel});const render=vi.fn(),hand=vi.fn(),pose=vi.fn();scheduler.subscribeRenderFrame(render);scheduler.subscribeHandFrame(hand);scheduler.subscribePoseFrame(pose);scheduler.start();for(at=0;at<=200;at+=10)callback(at);expect(render.mock.calls.length).toBeGreaterThanOrEqual(10);expect(hand).toHaveBeenCalledTimes(5);expect(pose).toHaveBeenCalledTimes(3);scheduler.pause();at=300;callback(at);expect(pose).toHaveBeenCalledTimes(3);scheduler.resume();at=310;callback(at);expect(pose).toHaveBeenCalledTimes(4);scheduler.dispose();expect(cancel).toHaveBeenCalledWith(7);});});
