# 백엔드 성능 개선 검토

## 1. 검토 결과 요약

배포 설정과 운영 코드를 검토한 결과, 성능 개선 후보 8개와 신규 배포를 막을 수 있는 스키마 문제 1개를 확인했다.

가장 먼저 처리할 항목은 다음과 같다.

1. 신규 배포 시 발생할 수 있는 초기 스키마와 Flyway 마이그레이션 충돌
2. 랭킹 조회의 `game_results` 전체 스캔 및 JVM 집계
3. WebSocket 메시지마다 발생하는 DB 조회
4. SSE 전체 목록의 동기식 순차 전송
5. 솔로 결과 조회의 무제한 응답과 N+1 쿼리

검토 범위는 다음과 같다.

- 루트 배포 설정: `.gitlab-ci.yml`, `docker-compose.yml`, `.dockerignore`
- 애플리케이션 빌드·실행 설정: `Dockerfile`, `build.gradle`, `settings.gradle`, Gradle Wrapper, `env.sample`
- 운영 환경 설정: `application.yaml`
- DB 초기화 및 마이그레이션 SQL 10개
- `src/main/java` 운영 Java 파일 140개
- 관련 성능·통합 테스트

코드는 변경하지 않고 진단만 수행했다.

---

## 2. 최우선 조치: 신규 배포 스키마 충돌

### 현재 구조

`docker-compose.yml`은 다음 초기화 스크립트를 MySQL 컨테이너에 마운트한다.

```yaml
- /opt/sudal/schema.sql:/docker-entrypoint-initdb.d/001-schema.sql:ro
```

GitLab CI는 배포 시 다음 파일을 `/opt/sudal/schema.sql`로 복사한다.

```text
backend/suhwa/scripts/schema.sql
```

문제는 이 `schema.sql`이 현재 Flyway V1의 최종 스키마보다 오래된 구조라는 점이다.

- `scripts/schema.sql`에는 `game_results` 테이블이 없다.
- `application.yaml`은 `baseline-on-migrate: true`, `baseline-version: 1`로 설정되어 있다.
- 비어 있지 않은 신규 DB에서는 Flyway가 V1을 실행하지 않고 baseline 처리할 수 있다.
- 이후 V6는 존재한다고 가정한 `game_results` 테이블에 컬럼과 제약조건을 추가한다.

관련 파일:

- `docker-compose.yml:10`
- `.gitlab-ci.yml:111`
- `backend/suhwa/scripts/schema.sql`
- `backend/suhwa/src/main/resources/application.yaml:45-48`
- `backend/suhwa/src/main/resources/db/migration/V6__activity_reward_sessions.sql:95`

### 영향

새 MySQL 볼륨을 사용하는 최초 배포나 재해복구 배포에서 다음 순서로 실패할 가능성이 높다.

1. 구형 `schema.sql`로 일부 테이블 생성
2. Flyway가 V1을 실행하지 않고 baseline 처리
3. V2, V4, V5 실행
4. V6에서 `game_results`를 찾지 못해 애플리케이션 부팅 실패

### 권장 조치

- `schema.sql` 마운트와 GitLab CI의 복사 단계를 제거한다.
- MySQL 컨테이너는 데이터베이스만 생성하도록 한다.
- 실제 테이블 생성과 변경은 Flyway V1~V6만 담당하도록 단일화한다.
- 운영 DB 변경 전에는 반드시 다음을 확인한다.
  - DB 백업
  - 실제 테이블과 인덱스
  - `flyway_schema_history`
  - 현재 적용된 수동 SQL 이력

스키마 정의를 `schema.sql`과 Flyway 양쪽에서 관리하면 같은 문제가 다시 발생할 수 있으므로 Flyway를 단일 진실 공급원으로 사용하는 것이 적절하다.

---

## 3. 성능 개선 우선순위

## 3.1 랭킹 조회 전체 스캔

우선순위: 매우 높음

### 현재 구조

`RankingService.getRankings()`는 다음 절차로 랭킹을 계산한다.

1. 선택한 게임 종류의 `game_results` 전체 행 조회
2. JVM에서 사용자별 그룹화
3. 활성 사용자 전체 조회
4. JVM에서 SUM 또는 최소 플레이 시간 계산
5. 전체 사용자 정렬
6. 상위 5명과 요청자 순위 계산

관련 파일:

- `ranking/service/RankingService.java:35-64`
- `gameresult/service/GameResultService.java:59-60`
- `gameresult/repository/GameResultRepository.java:12`

### 영향

`game_results`는 대전 한 판당 2행씩 계속 누적된다. 데이터가 증가하면 다음 비용이 선형으로 증가한다.

- DB가 애플리케이션으로 전송하는 데이터 크기
- JPA 엔티티 생성 비용
- JVM 힙 사용량
- GC 부하
- 그룹화와 정렬 시간
- 응답 시간

현재 쿼리 timeout이 3초이므로 데이터가 충분히 쌓이면 단순 지연이 아니라 요청 실패로 이어질 수 있다.

### 권장 조치

집계와 정렬을 DB에서 수행한다.

대전 랭킹 예시:

```sql
SELECT
    gr.user_id,
    SUM(gr.score) AS win_count,
    COUNT(*) - SUM(gr.score) AS loss_count
FROM game_results gr
JOIN users u ON u.id = gr.user_id
WHERE gr.game_type = ?
  AND u.deleted_at IS NULL
GROUP BY gr.user_id
ORDER BY win_count DESC, loss_count ASC
LIMIT 5;
```

추가 작업:

- Top 5 전용 projection 쿼리 작성
- 요청자 순위 전용 집계 쿼리 또는 MySQL Window Function 적용
- 대전 랭킹 인덱스 검토:
  - `(game_type, user_id, score)`
- 솔로 랭킹 인덱스 검토:
  - `(game_type, user_id, play_duration_ms)`
- 실제 MySQL `EXPLAIN ANALYZE`로 실행 계획 확인

---

## 3.2 WebSocket 메시지마다 DB 조회

우선순위: 매우 높음

### 현재 구조

`GameRoomWebSocketHandler.handleTextMessage()`는 메시지 종류를 파싱하기 전에 `isStillParticipant()`를 실행한다.

`isStillParticipant()`는 매번 다음 DB 조회를 수행한다.

```java
gameRoomRepository.findById(roomId)
```

관련 파일:

- `game/realtime/GameRoomWebSocketHandler.java:122-169`

### 영향

WebRTC 연결 과정에서는 다음 메시지가 짧은 시간에 연속으로 전송된다.

- offer
- answer
- 여러 ICE candidate
- 연결 상태 메시지

따라서 실제 게임 상태를 변경하지 않는 단순 중계 메시지에도 SQL이 실행된다. 동시 통화가 증가하면 최대 크기 10인 Hikari 커넥션 풀이 WebSocket 중계 트래픽에 의해 소진될 수 있다.

### 권장 조치

- WebSocket 핸드셰이크 시 DB에서 참여자를 한 번 검증한다.
- 연결 이후에는 `RoomParticipantRegistry`의 인메모리 상태로 참여 여부를 확인한다.
- 방 종료·퇴장 트랜잭션 커밋 후 인메모리 상태를 제거하거나 무효화한다.
- 상태 변경 REST API에 대해서만 DB를 기준으로 검증한다.
- WebSocket 입력에 다음 제한을 추가한다.
  - 최대 메시지 크기
  - 사용자별 초당 메시지 수
  - 허용된 메시지 타입
  - payload 최대 깊이 및 크기

다중 인스턴스로 확장한다면 인메모리 상태만으로는 부족하므로 sticky session 또는 Redis/pub-sub 같은 공유 상태가 필요하다.

---

## 3.3 SSE 전체 목록 동기식 순차 전송

우선순위: 매우 높음

### 현재 구조

`LobbyBroadcastService.broadcastUpdate()`는 게임방 변경마다 다음 작업을 수행한다.

1. 모든 WAITING 게임방 조회
2. 전체 게임방 스냅샷 생성
3. 모든 SSE 구독자에게 순차적으로 전송

관련 파일:

- `game/realtime/LobbyBroadcastService.java:25-37`
- `game/service/GameRoomService.java:92`
- `game/service/GameRoomService.java:119`
- `game/service/GameRoomService.java:175`
- `game/service/GameRoomService.java:211`
- `game/service/GameRoomService.java:236`

`broadcastUpdate()`는 트랜잭션의 `afterCommit`에서 실행되지만 별도 비동기 실행기가 없으므로 실제 전송은 요청 스레드에서 처리된다.

### 영향

- 느린 SSE 클라이언트 하나가 방 생성·입장·퇴장 API 응답을 지연시킬 수 있다.
- 구독자 수가 증가하면 전송 시간이 선형으로 증가한다.
- 변경마다 전체 방 목록을 직렬화하고 전송한다.
- 15초 하트비트도 공용 스케줄러 스레드에서 순차 전송된다.
- 느린 전송이 쌓이면 예약된 퇴장 및 방 정리 작업도 지연될 수 있다.

### 권장 조치

- SSE 전송용 bounded executor를 별도로 둔다.
- 느린 구독자에 전송 timeout과 backpressure 정책을 적용한다.
- 짧은 시간 동안 발생한 여러 변경을 50~200ms debounce로 합친다.
- 가능하면 전체 스냅샷 대신 다음 delta 이벤트를 전송한다.
  - ROOM_CREATED
  - ROOM_UPDATED
  - ROOM_REMOVED
- 하트비트 전송과 게임방 예약 작업의 스케줄러를 분리한다.
- 구독자별 전송 큐 크기에 상한을 둔다.

---

## 3.4 솔로 결과 무제한 조회 및 N+1 쿼리

우선순위: 높음

### 현재 구조

`SoloSessionService.findCompleted()`는 다음 작업을 수행한다.

1. 사용자의 완료된 솔로 세션 전체 조회
2. 각 세션마다 `solo_symbol_statistics`를 개별 조회

관련 파일:

- `gameresult/service/SoloSessionService.java:128-136`
- `gameresult/repository/SoloSessionRepository.java:18`
- `gameresult/repository/SoloSymbolStatisticRepository.java:9`

### 영향

결과가 N개이면 `1 + N`개의 SQL이 실행된다.

또한 페이지 제한이 없으므로 사용 기록이 증가할수록 다음 비용이 증가한다.

- DB 조회량
- SQL 호출 횟수
- 응답 JSON 크기
- 서버 메모리
- 네트워크 전송량
- 클라이언트 렌더링 비용

### 권장 조치

- 커서 기반 페이지네이션 적용
- 기본 페이지 크기 20개 정도로 제한
- 한 페이지의 세션 ID를 수집
- `WHERE solo_session_id IN (...)` 쿼리로 통계를 한 번에 조회
- 조회한 통계를 JVM에서 세션 ID별로 그룹화

이 방식이면 페이지당 SQL을 다음 2개로 제한할 수 있다.

1. 솔로 세션 페이지 조회
2. 해당 세션들의 통계 일괄 조회

---

## 3.5 요청 리스트 크기 제한 및 개별 INSERT

우선순위: 높음

### 현재 구조

다음 요청 리스트에는 최대 크기 제한이 없다.

- `StartSoloSessionRequest.symbolRange`
- `CompleteSoloSessionRequest.symbolStatistics`

관련 파일:

- `gameresult/dto/StartSoloSessionRequest.java:9-12`
- `gameresult/dto/CompleteSoloSessionRequest.java:8-14`

서비스에서는 각 항목을 반복하면서 개별 `save()`한다.

관련 파일:

- `gameresult/service/SoloSessionService.java:61-67`
- `gameresult/service/SoloSessionService.java:103-113`

### 영향

클라이언트가 큰 배열을 전송하면 하나의 요청으로 다음 자원을 과도하게 사용할 수 있다.

- JSON 파싱 메모리
- 검증 CPU
- DB INSERT 수
- 트랜잭션 시간
- DB 커넥션 점유 시간

문자열 길이도 DB 컬럼 길이에 맞는 검증이 부족하다.

- `difficulty`: DB `VARCHAR(50)`
- `symbol`: DB `VARCHAR(20)`

### 권장 조치

- 리스트 필드에 `@Size(max = ...)` 적용
- 문자열에도 DB 컬럼과 동일한 최대 길이 검증 적용
- 실제 도메인에 맞는 최대 기호 개수 정의
- 개별 `save()` 대신 일괄 저장 검토
- 필요하면 Spring Data JPA 대신 JDBC batch 사용

`hibernate.jdbc.batch_size=30`이 설정되어 있지만 많은 엔티티가 `GenerationType.IDENTITY`를 사용하므로 INSERT batching 효과가 제한될 수 있다. ID 생성 전략 또는 자연키·복합키 사용 가능성도 함께 검토해야 한다.

---

## 3.6 Refresh Token 테이블 무한 증가

우선순위: 중간

### 현재 구조

Refresh Token은 발급과 폐기만 수행하고, 만료되거나 폐기된 행을 삭제하지 않는다.

관련 파일:

- `auth/service/RefreshTokenService.java`
- `auth/repository/RefreshTokenRepository.java`
- `auth/domain/RefreshToken.java`

토큰 회전마다 새 행이 생성되고 기존 행에는 `revoked_at`만 설정된다.

### 영향

- 테이블과 인덱스 크기 지속 증가
- 토큰 조회 및 갱신 비용 증가
- 백업 및 복구 시간 증가
- 스토리지 사용량 증가
- 사용자 전체 토큰 폐기 UPDATE 범위 증가

### 권장 조치

- 만료·폐기된 토큰을 주기적으로 batch delete
- 한 번에 500~1,000건 정도만 삭제
- 다음 인덱스 검토
  - `expires_at`
  - `revoked_at`
- 삭제 기준에 짧은 보존 기간을 두어 장애 분석에 필요한 이력을 유지
- 대량 삭제 시 긴 트랜잭션이 되지 않도록 반복 처리

---

## 3.7 JWT 중복 파싱 및 활성 사용자 중복 조회

우선순위: 중간

### JWT 중복 파싱

현재 필터는 같은 토큰을 두 번 파싱한다.

1. `validateToken(token)`
2. `getUserId(token)`

관련 파일:

- `common/security/JwtAuthenticationFilter.java:32-34`
- `common/security/JwtTokenProvider.java:37-55`

또한 `parseClaims()` 호출마다 JJWT parser를 새로 생성한다.

### 권장 조치

- 애플리케이션 시작 시 parser를 한 번 생성해 필드로 보관
- 단일 `parseAndValidate()` 결과에서 사용자 ID를 추출
- 검증과 사용자 ID 추출을 하나의 서명 검증으로 처리

### 활성 사용자 중복 조회

일부 완료 API는 이미 `userService.getActiveUser(userId)`를 호출한 후 `growthRewardService.lockPet(userId)`를 호출한다.

`lockPet()` 내부에서 다시 `getActiveUser()`를 실행한다.

대표 경로:

- `PracticeSessionService.complete()`
- `TestSessionService.completeTest()`
- `SoloSessionService.complete()`
- `GrowthRewardService.lockPet()`

### 권장 조치

- 호출자가 이미 사용자를 검증했다면 사용자 재조회를 생략한다.
- `lockPetValidatedUser()`와 같은 내부 전용 메서드를 분리한다.
- 또는 사용자 활성 여부와 펫 잠금을 한 쿼리로 확인하는 projection/join 방식 검토

---

## 3.8 게임방 정리 작업의 대량 적재

우선순위: 중간

### 현재 구조

`GameRoomCleanupScheduler.cleanupStaleRooms()`는 오래된 방을 모두 조회해 메모리에 올린 후 하나의 큰 `IN (...)` DELETE를 실행한다.

관련 파일:

- `game/scheduler/GameRoomCleanupScheduler.java:51-70`
- `game/repository/GameRoomRepository.java:20-44`

평상시에는 정리 대상이 적을 수 있지만 다음 상황에서는 대상이 급격히 증가할 수 있다.

- 서버 장기간 중단
- 스케줄러 장애
- DB 일시 장애
- 대규모 테스트 방 생성

### 영향

- 큰 결과 목록의 메모리 적재
- 매우 긴 `IN` 절
- 30초 정리 트랜잭션 timeout
- DB lock 유지 시간 증가
- 정리 중 다른 게임방 요청 지연

### 권장 조치

- 500~1,000개 단위로 ID만 페이지 조회
- 작은 batch 단위로 DELETE
- batch 사이에 트랜잭션 커밋
- 한 번의 스케줄 실행에서 처리할 최대 개수 설정
- 남은 대상은 다음 실행에서 계속 처리

현재 적용된 `(status, updated_at)` 복합 인덱스는 유지해야 한다.

---

## 4. 배포 및 빌드 성능 개선

## 4.1 `.dockerignore` 적용 위치

현재 `.dockerignore`는 저장소 루트에 있지만 Docker 빌드 컨텍스트는 다음과 같다.

```text
./backend/suhwa
```

따라서 루트 `.dockerignore`는 이 빌드에 적용되지 않는다.

관련 파일:

- `.dockerignore`
- `.gitlab-ci.yml:51-54`

### 영향

다음 파일이 Docker daemon으로 불필요하게 전송될 수 있다.

- `build/`
- `bin/`
- `.gradle/`
- 로컬 `.env`
- IDE 설정
- 로그

Dockerfile이 해당 파일을 이미지로 `COPY`하지 않더라도 빌드 컨텍스트 전송 시간과 보안 위험은 남는다.

### 권장 조치

`backend/suhwa/.dockerignore`를 추가한다.

권장 제외 대상:

```text
.gradle/
build/
bin/
.env*
*.log*
.idea/
.vscode/
```

---

## 4.2 Docker Gradle 의존성 캐시

현재 Dockerfile은 `src`를 복사한 뒤 `bootJar`를 실행한다.

```dockerfile
COPY src ./src
RUN ./gradlew clean bootJar --no-daemon
```

소스 파일 하나만 변경해도 Gradle 실행 레이어가 무효화되고 의존성 캐시를 재사용하기 어렵다.

### 권장 조치

- BuildKit cache mount로 `/root/.gradle` 재사용
- 컨테이너 빌드에서는 불필요한 `clean` 제거
- 의존성 해석 레이어와 소스 빌드 레이어 분리 검토

예시:

```dockerfile
RUN --mount=type=cache,target=/root/.gradle \
    ./gradlew bootJar --no-daemon
```

---

## 4.3 컨테이너 자원 제한

`docker-compose.yml`에는 다음 제한이 없다.

- 백엔드 JVM 메모리
- 백엔드 CPU
- MySQL 메모리
- MySQL CPU

### 영향

백엔드와 MySQL이 같은 호스트에서 서로의 메모리를 압박할 수 있다. 특히 JVM이 호스트 전체 메모리를 기준으로 힙을 크게 잡으면 MySQL buffer pool과 운영체제 page cache가 부족해질 수 있다.

### 권장 조치

- 컨테이너별 메모리·CPU 제한
- `JAVA_TOOL_OPTIONS`로 최대 힙 비율 또는 `-Xmx` 지정
- MySQL `innodb_buffer_pool_size`를 호스트 메모리에 맞게 설정
- Hikari 최대 풀 크기는 부하 테스트 결과로 결정
- `DB_POOL_MAX_SIZE`를 운영 환경변수로 명시

현재 기본값 10을 무조건 늘리면 DB 경합만 증가할 수 있으므로 측정 후 조정해야 한다.

---

## 4.4 로그 로테이션

Compose에 Docker 로그 크기 제한이 없다.

### 영향

Docker 기본 JSON 로그가 계속 증가하면 다음 문제가 발생할 수 있다.

- 디스크 부족
- 컨테이너 로그 조회 지연
- 파일시스템 I/O 증가
- 최악의 경우 MySQL 데이터 쓰기 실패

### 권장 조치

백엔드와 MySQL에 로그 제한을 추가한다.

```yaml
logging:
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"
```

---

## 4.5 백엔드 healthcheck와 배포 방식

현재 MySQL에는 healthcheck가 있지만 백엔드 컨테이너에는 healthcheck가 없다.

GitLab CI는 외부 반복 요청으로 상태를 확인하지만 실패하면 기존 컨테이너가 이미 교체된 상태다.

관련 파일:

- `docker-compose.yml`
- `.gitlab-ci.yml:119-141`

### 권장 조치

- 백엔드 서비스에도 `/actuator/health` 기반 healthcheck 추가
- 새 컨테이너가 정상 상태가 된 후 이전 컨테이너 종료
- 실패 시 이전 이미지로 자동 롤백
- 최소한 직전 정상 이미지 태그를 별도로 유지
- `latest`만 사용하지 말고 커밋 SHA 이미지로 배포

---

## 4.6 수평 확장 제한

현재 다음 상태가 애플리케이션 메모리에 저장된다.

- SSE 구독자
- WebSocket 세션
- 게임방 참여자의 실시간 상태
- 실시간 일회용 ticket
- 예약된 퇴장 task

또한 Compose는 `container_name`을 고정한다.

### 영향

백엔드 인스턴스를 2개 이상 실행하면 다음 문제가 발생할 수 있다.

- 다른 인스턴스에서 발급한 ticket을 소비하지 못함
- 같은 방 참여자가 서로 다른 인스턴스에 연결됨
- SSE 이벤트가 일부 구독자에게만 전달됨
- 예약된 퇴장 상태 불일치

### 권장 조치

단일 인스턴스를 유지할 경우:

- 최대 동시 연결 수를 측정
- CPU, 메모리, DB pool 한계를 명확히 설정

다중 인스턴스로 확장할 경우:

- sticky session
- Redis 기반 ticket 저장
- Redis pub/sub 또는 메시지 브로커
- 분산 가능한 게임방 상태 모델
- 고정 `container_name` 제거

---

## 5. 관측성 개선

현재 Actuator는 health만 외부에 노출한다.

성능 문제를 실제로 확인하려면 다음 지표가 필요하다.

### JVM

- Heap 사용량
- GC 횟수와 pause
- 스레드 수
- CPU 사용률

### HTTP

- 엔드포인트별 요청 수
- 평균 응답 시간
- p95, p99 응답 시간
- 4xx, 5xx 비율

### DB와 Hikari

- active connection
- idle connection
- pending connection
- connection acquire 시간
- 쿼리 timeout 수
- slow query

### 실시간 기능

- SSE 구독자 수
- SSE 전송 실패 수
- SSE 구독자별 대기 큐 크기
- WebSocket 연결 수
- WebSocket 초당 메시지 수
- 사용자별 메시지 rate limit 거부 수
- 예약된 퇴장 task 수
- 스케줄러 active thread 및 queue

### 권장 조치

- Micrometer Prometheus Registry 추가
- `/actuator/prometheus`를 내부망 또는 인증된 경로로 노출
- Grafana 대시보드 구성
- 랭킹, 솔로 결과, SSE, WebSocket에 별도 timer/counter 적용

---

## 6. 이미 적용이 잘 된 부분

다음 항목은 현재 구조에서 유지하는 것이 좋다.

### DB 및 JPA

- `spring.jpa.open-in-view: false`
- Hikari 최대 풀 크기 제한
- DB connection timeout
- JPA query timeout
- transaction timeout
- Hibernate INSERT/UPDATE ordering

### 쿼리 최적화

- 오답 목록 DTO projection 단일 쿼리
- 최근 오답과 최근 테스트 조회의 페이지 제한
- 게임방 정리용 `(status, updated_at)` 복합 인덱스
- 게임방 bulk delete

### 캐시

- 변경이 거의 없는 수어 콘텐츠의 Caffeine 캐시
- 캐시 최대 크기 제한

### 실시간 및 스케줄링

- 단일 기본 스케줄러가 아닌 4개 스레드 풀 사용
- SSE heartbeat
- 종료된 게임방의 인메모리 상태 정리
- 트랜잭션 커밋 이후 실시간 알림 전송

### 컨테이너

- Docker 멀티 스테이지 빌드
- JDK와 JRE 이미지 분리
- non-root 사용자 실행
- MySQL 영속 볼륨
- MySQL healthcheck

---

## 7. 테스트 검증 결과

격리된 H2 설정으로 전체 테스트를 실행했다.

```text
tests=233
failures=0
errors=0
skipped=3
```

### 테스트 실행 중 확인된 문제

`build.gradle`은 로컬 `.env`가 존재하면 해당 값을 모든 Test task에 주입한다.

관련 파일:

- `build.gradle:84-101`

따라서 로컬 `.env`가 MySQL을 가리키고 MySQL이 실행 중이지 않으면 일반 단위·통합 테스트까지 DB 연결 실패로 무너진다.

### 권장 조치

- `src/test/resources/application-test.yaml` 추가
- 테스트 기본 DB를 H2로 고정
- `@ActiveProfiles("test")` 또는 Gradle Test task에서 test profile 지정
- 실제 MySQL 검증 테스트만 Testcontainers 사용
- 개발자의 `.env`를 일반 테스트에 자동 주입하지 않도록 변경

이렇게 하면 로컬 환경에 영향을 받지 않는 재현 가능한 테스트와 더 빠른 피드백을 얻을 수 있다.

---

## 8. 권장 적용 순서

### 1단계: 배포 안정성

1. `schema.sql`과 Flyway 충돌 제거
2. 신규 빈 DB에서 실제 Compose 부팅 검증
3. 백엔드 healthcheck와 실패 롤백 구성

### 2단계: DB 병목 제거

1. 랭킹 DB 집계 쿼리로 변경
2. 솔로 결과 페이지네이션 및 N+1 제거
3. 요청 리스트 크기 제한
4. Refresh Token 정리 작업 추가
5. 게임방 cleanup batch 처리

### 3단계: 실시간 경로 개선

1. WebSocket SIGNAL 메시지의 DB 조회 제거
2. 메시지 크기 및 rate limit
3. SSE 비동기 전송
4. SSE 변경 이벤트 debounce 또는 delta 적용

### 4단계: 작은 반복 비용 제거

1. JWT 단일 파싱
2. parser 재사용
3. 활성 사용자 중복 조회 제거
4. 실제 효과가 있는 INSERT batching 적용

### 5단계: 운영 튜닝

1. Prometheus/Grafana 지표 추가
2. 컨테이너 메모리와 CPU 제한
3. JVM, MySQL, Hikari 부하 테스트
4. Docker 빌드 캐시 및 `.dockerignore` 정리
5. 로그 로테이션 적용

